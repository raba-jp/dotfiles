set -U EDITOR nvim
# Alias
alias vi 'nvim'
alias vim 'nvim'
alias sudo 'sudo '
alias ll 'ls -alG'
alias untar 'tar -xzvf'
alias tmux 'tmux -f $XDG_CONFIG_HOME/tmux/tmux.conf'
alias cp 'cp -i'
alias mv 'mv -i'
alias rm 'rm -i'
